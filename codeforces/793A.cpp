#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    int n,k;
    cin >> n >> k;
    int a,b,c;
    int steps = INT_MAX;
    cin >> a >> b>>c;

    for(int i=0;i<)

    return 0;
}