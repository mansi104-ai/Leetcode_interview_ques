#include <iostream>
#include <algorithm>
using namespace std;

class Solution{
    public:
      ListNode* sortlist(ListNode* head){
           if(!head || !head->next){
            return head;
           }

           ListNode* curr = head;
           

      }
};